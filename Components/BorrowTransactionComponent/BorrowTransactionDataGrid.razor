

<RadzenStack Gap="16">
  <DataGrid ID="BorrowTransactionDataGrid" @ref="@dataGrid" TItem="JsonObject" LoadData="LoadData" AllowSelected="true" 
    RowLink=@(row => $"/borrowtransaction/transaction/{row["ID"]}")>
    <!-- #region Toolbar -->
    <Buttons>
      <RadzenRow Gap="8">
        @* <RoleAccess Code="RC0003838"> *@
        @* Belum di set untuk RoleCode *@
          <Button ButtonStyle="ButtonStyle.Info" Text="Add" Icon="add" Click="Add" />
        @* </RoleAccess> *@

        @* <RoleAccess Code="RD0000966"> *@
        @* Belum di set untuk RoleCode *@
          <Button ButtonStyle="ButtonStyle.Danger" Text="Delete" Icon="delete" Click="Delete" />
        @* </RoleAccess> *@
      </RadzenRow>
    </Buttons>
    <!-- #endregion -->

    <!-- #region Columns -->
    <Columns>
    <DataGridColumn TItem="JsonObject" Property="Code" Title="Code" Width="25%" />
      <DataGridColumn TItem="JsonObject" Property="BorrowDate" Title="Date" Width="25%" />
      <DataGridColumn TItem="JsonObject" Property="BorrowerName" Title="Borrower" Width="25%" />
      <DataGridColumn TItem="JsonObject" Property="Status" Title="Status" Width="25%" />
    </Columns>
    <!-- #endregion -->
  </DataGrid>
</RadzenStack>

